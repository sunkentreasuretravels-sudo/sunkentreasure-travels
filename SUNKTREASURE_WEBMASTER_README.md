# SunkenTreasureWebMaster — Corrected Integration

This build uses the existing Sunken Treasure Travels website as the brand source of truth.

The WorldWebMaster engine is added under `/worldwebmaster/`.

Important:
- Do not replace `index.html` with a framework-only homepage.
- Do not delete existing Sunken Treasure Travels content.
- WorldWebMaster is additive infrastructure.
- Future brands can use the same engine independently.
